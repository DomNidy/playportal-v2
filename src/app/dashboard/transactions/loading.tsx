import Typography from "~/components/ui/Typography";

export default function LoadingPage() {
  return (
    <div>
      <Typography variant={"h1"} affects={"large"}>
        Loading...
      </Typography>
    </div>
  );
}
